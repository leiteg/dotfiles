# /home/leite/.config/tmuxinator/work.yml

name: work
root: /scratch/llvm-project

windows:
  - llvm:
      root: /scratch/llvm-project
  - tests:
      root: /scratch/runtime/tests
  - task-bench:
      root: /scratch/task-bench
