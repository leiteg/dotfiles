# /home/leite/.config/tmuxinator/work.yml

name: work
root: /scratch/llvm

windows:
  - llvm:
      root: /scratch/llvm
  - taskbench:
      root: /scratch/taskbench
  - ompcbench:
      root: /scratch/ompcbench
      layout: main-vertical
      panes:
        - source venv/bin/activate
        - source venv/bin/activate && vim
